# پروژه تمرین Git و GitHub

یک پروژه تمرینی ساده برای یادگیری Git، GitHub و توسعه وب با HTML، CSS و JavaScript.

## 🚀 ویژگی‌ها

- ✅ قالب ساده و تمیز HTML/CSS/JS
- ✅ طراحی کاملاً ریسپانسیو (واکنش‌گرا)
- ✅ دارای افکت‌های تعاملی و انیمیشن‌های روان
- ✅ سیستم تغییر تم (روشن/تاریک)
- ✅ شبیه‌ساز کامیت‌های Git
- ✅ مناسب برای تمرین Git و GitHub
- ✅ کاملاً فارسی و راست‌چین

## 📁 ساختار پروژه

```
test/
├── index.html          # فایل اصلی HTML
├── README.md           # همین فایل توضیحات
├── .gitignore          # فایل‌های نادیده گرفته شده توسط Git
└── assets/             # پوشه برای فایل‌های استاتیک
    ├── css/
    │   └── style.css   # فایل استایل‌ها (اختیاری)
    └── js/
        └── app.js      # فایل جاوااسکریپت (اختیاری)
```

## 🛠️ تکنولوژی‌های استفاده شده

- **HTML5** - ساختار صفحه
- **CSS3** - استایل‌دهی و انیمیشن‌ها
- **JavaScript** - تعاملات و منطق
- **Font Awesome** - آیکون‌ها
- **Google Fonts** - فونت Vazirmatn فارسی

## 🎯 هدف پروژه

این پروژه برای تمرین و یادگیری موارد زیر طراحی شده است:

1. **Git Basics**: کامیت، برنچ، مرج، پوش
2. **GitHub Workflow**: Fork، Pull Request، Issues
3. **Frontend Development**: HTML، CSS، JavaScript
4. **Responsive Design**: طراحی واکنش‌گرا
5. **GitHub Pages**: دپلوی سایت استاتیک

## 🚦 شروع کار

### 1. کلون کردن پروژه

```bash
git clone https://github.com/PaKiani/test.git
cd test
```

### 2. باز کردن در مرورگر

فایل `index.html` را مستقیماً در مرورگر باز کنید یا از یک Live Server استفاده کنید.

### 3. ساخت تغییرات و کامیت

```bash
# مشاهده وضعیت فایل‌ها
git status

# اضافه کردن تغییرات
git add .

# ثبت تغییرات
git commit -m "توضیح تغییرات"

# ارسال به GitHub
git push origin main
```

## 🔧 ویژگی‌های تعاملی پروژه

### 1. تغییر رنگ هدر
کلیک روی دکمه "تغییر رنگ هدر" رنگ پس‌زمینه هدر را تغییر می‌دهد.

### 2. شبیه‌ساز کامیت Git
کلیک روی دکمه "شبیه‌سازی یک کامیت" یک کامیت شبیه‌سازی شده به تاریخچه اضافه می‌کند.

### 3. منوی همبرگری
در دستگاه‌های موبایل، منوی ناوبری به صورت همبرگری نمایش داده می‌شود.

### 4. واکنش‌گرایی
سایت در تمام دستگاه‌ها از موبایل تا دسکتاپ به خوبی نمایش داده می‌شود.

## 📱 ریسپانسیو بودن

- 📱 موبایل: ۰ تا ۷۶۸px
- 🖥️ تبلت: ۷۶۸px تا ۱۰۲۴px
- 🖥️ دسکتاپ: ۱۰۲۴px به بالا

## 🎨 طراحی

### رنگ‌ها
- اصلی: `#6a11cb` تا `#2575fc` (گرادیان)
- متن: `#333` برای متن اصلی
- پس‌زمینه: `#f5f7fa`
- کارت‌ها: `#ffffff`

### فونت‌ها
- اصلی: Vazirmatn (فارسی)
- کدها: سیستم‌فونت‌های مونواسپیس

### انیمیشن‌ها
- ترنزیشن‌های نرم برای تمام تعاملات
- انیمیشن fadeIn برای کارت‌ها
- افکت hover روی دکمه‌ها و کارت‌ها

## 🏗️ توسعه

### افزودن ویژگی جدید
1. یک برنچ جدید ایجاد کنید:
   ```bash
   git checkout -b feature/new-feature
   ```

2. تغییرات را اعمال و کامیت کنید:
   ```bash
   git add .
   git commit -m "اضافه کردن ویژگی جدید"
   ```

3. برنچ را به GitHub پوش کنید:
   ```bash
   git push origin feature/new-feature
   ```

4. یک Pull Request ایجاد کنید.

### ساخت برنچ‌های تمرینی
```bash
# ایجاد برنچ‌های مختلف برای تمرین
git branch exercise-1
git branch exercise-2
git branch exercise-3

# جابجایی بین برنچ‌ها
git checkout exercise-1

# ادغام برنچ با main
git checkout main
git merge exercise-1
```

## 🌐 دپلوی در GitHub Pages

### روش ۱: از طریق تنظیمات GitHub
1. به مخزن در GitHub بروید
2. Settings > Pages را انتخاب کنید
3. در بخش Source، برنچ `main` و پوشه `/ (root)` را انتخاب کنید
4. Save را بزنید

### روش ۲: از طریق gh-pages برنچ
```bash
# ایجاد برنچ gh-pages
git checkout -b gh-pages

# پوش کردن به GitHub
git push origin gh-pages
```

آدرس سایت شما خواهد بود: `https://pakiani.github.io/test/`

## 📝 دستورات Git برای تمرین

### دستورات پایه
```bash
# شروع یک ریپازیتوری جدید
git init

# مشاهده وضعیت فایل‌ها
git status

# اضافه کردن فایل‌ها
git add filename.html
git add .  # تمام فایل‌ها

# ثبت تغییرات
git commit -m "پیام کامیت"

# مشاهده تاریخچه کامیت‌ها
git log
git log --oneline  # نمایش خلاصه

# حذف تغییرات stage شده
git reset HEAD filename
```

### کار با برنچ‌ها
```bash
# مشاهده برنچ‌ها
git branch
git branch -a  # همه برنچ‌ها

# ایجاد برنچ جدید
git branch new-feature

# جابجایی بین برنچ‌ها
git checkout new-feature
git checkout -b new-feature  # ایجاد و جابجایی

# ادغام برنچ‌ها
git checkout main
git merge new-feature

# حذف برنچ
git branch -d new-feature
```

### کار با GitHub
```bash
# اضافه کردن remote
git remote add origin https://github.com/PaKiani/test.git

# ارسال به GitHub
git push origin main
git push origin branch-name

# دریافت تغییرات
git pull origin main

# کلون کردن ریپازیتوری
git clone https://github.com/PaKiani/test.git
```

## 🐛 عیب‌یابی

### اگر GitHub Pages کار نمی‌کند:
1. مطمئن شوید فایل `index.html` در ریشه پروژه است
2. برنچ `main` یا `gh-pages` را در تنظیمات Pages انتخاب کنید
3. چند دقیقه منتظر بمانید (گاهی تا ۱۰ دقیقه طول می‌کشد)

### اگر تغییرات نمایش داده نمی‌شود:
1. کش مرورگر را پاک کنید (Ctrl + Shift + R)
2. از DevTools (F12) برای بررسی خطاها استفاده کنید
3. مطمئن شوید فایل‌ها به درستی آپلود شده‌اند

### اگر انیمیشن‌ها کار نمی‌کنند:
1. مطمئن شوید JavaScript در مرورگر فعال است
2. خطاهای کنسول را بررسی کنید
3. فایل‌ها را از یک سرور محلی اجرا کنید

## 🤝 مشارکت

این پروژه برای تمرین طراحی شده است. می‌توانید:

1. پروژه را Fork کنید
2. تغییرات خود را اعمال کنید
3. Pull Request ارسال کنید
4. Issues ایجاد کنید

## 📄 لایسنس

این پروژه برای اهداف آموزشی است و استفاده از آن آزاد می‌باشد.

## 📞 تماس

برای سوالات و پیشنهادات می‌توانید از طریق GitHub Issues در ارتباط باشید.

---

**نکته**: این پروژه به طور خاص برای تمرین Git و GitHub طراحی شده است. تمام بخش‌ها ساده و قابل توسعه هستند تا بتوانید روی آن تمرین کنید.

**شروع تمرین**: از امروز با این پروژه Git را یاد بگیرید و مهارت‌های خود را تقویت کنید! 🚀

## 🎯 تمرین‌های پیشنهادی

### سطح مبتدی
1. رنگ‌های سایت را تغییر دهید
2. یک بخش جدید به سایت اضافه کنید
3. فونت‌های متفاوت امتحان کنید

### سطح متوسط
1. یک فرم تماس به سایت اضافه کنید
2. دکمه تغییر تم (تاریک/روشن) ایجاد کنید
3. لودینگ انیمیشن برای دکمه‌ها بسازید

### سطح پیشرفته
1. یک سیستم کامنت‌گذاری ساده با localStorage بسازید
2. API گیت‌هاب را وصل کنید و اطلاعات ریپازیتوری را نمایش دهید
3. یک بازی ساده برای آموزش دستورات Git طراحی کنید

---

**موفق باشید در یادگیری Git و GitHub!** 💻
لینک (جهت مشاهده دمو)[https://pakiani.github.io/test/]✨
